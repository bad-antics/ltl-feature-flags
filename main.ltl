// ltl-feature-flags/main.ltl
//
// In-memory feature-flag evaluator with percentage rollout.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-feature-flags v0.1.0")
    io::println("In-memory feature-flag evaluator with percentage rollout.")
    0
}
